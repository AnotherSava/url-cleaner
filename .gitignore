# Build output
bin/
obj/
publish/

# Local docs (plans, drafts) — but allow GitHub Pages content through
docs/plans/
!docs/plans/completed/
!docs/plans/draft/

# Runtime config (generated on first run)
config.json

# Local deployment config override (replaces installed config.json on deploy)
config/local.json

# Scratch output (previews, screenshots)
/tmp/
